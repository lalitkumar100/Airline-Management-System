<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crimsonlogic.arilinemanangmentsystem.mapper.TicketMapper">

    <insert id="insertTicket">
        INSERT INTO tickets (ticket_id, booking_id, flight_id, seat_id, fare)
        VALUES (#{ticket.ticketId}, #{bookingId}, #{flightId}, #{seatId}, #{ticket.fare})
    </insert>

    <select id="selectTicketById" resultType="com.crimsonlogic.arilinemanangmentsystem.model.Ticket">
        SELECT ticket_id AS ticketId, fare 
        FROM tickets 
        WHERE ticket_id = #{ticketId}
    </select>
    
    <select id="selectTicketByBookingId" resultType="com.crimsonlogic.arilinemanangmentsystem.model.Ticket">
        SELECT ticket_id AS ticketId, fare 
        FROM tickets 
        WHERE booking_id = #{bookingId}
    </select>
    
    <select id="selectTicketsByFlightId" resultType="com.crimsonlogic.arilinemanangmentsystem.model.Ticket">
        SELECT ticket_id AS ticketId, fare 
        FROM tickets 
        WHERE flight_id = #{flightId}
    </select>

</mapper>
